<h1>Add a review to chosen restaurant</h1>

<%= simple_form_for [@restaurant, @review] do |f|%>
  <%= f.input :content %>
  <%= f.input :rating %>
  <button class= "btn btn-primary"><%= f.button :submit %></button>
<% end %>

